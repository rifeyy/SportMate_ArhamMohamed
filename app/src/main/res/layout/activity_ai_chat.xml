<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/chatRoot"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/sport_background"
    android:fitsSystemWindows="true">

    <!-- Header -->
    <LinearLayout
        android:id="@+id/chatHeader"
        android:layout_width="match_parent"
        android:layout_height="66dp"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingStart="10dp"
        android:paddingEnd="10dp"
        android:background="@color/sport_card">

        <Button
            android:id="@+id/btnOpenConversations"
            android:layout_width="82dp"
            android:layout_height="44dp"
            android:text="Menu"
            android:textAllCaps="false"
            android:textSize="12sp"
            android:textStyle="bold"
            android:textColor="@color/sport_primary_dark"
            android:background="@drawable/bg_button_secondary"
            android:backgroundTint="@null" />

        <TextView
            android:id="@+id/txtChatTitle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="SportMate AI"
            android:textSize="18sp"
            android:textStyle="bold"
            android:textColor="@color/sport_primary_dark"
            android:gravity="center"
            android:singleLine="true"
            android:ellipsize="end"
            android:paddingStart="8dp"
            android:paddingEnd="8dp" />

        <Space
            android:layout_width="82dp"
            android:layout_height="44dp" />

    </LinearLayout>

    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="@color/sport_border" />

    <!-- Chat messages -->
    <ScrollView
        android:id="@+id/chatScrollView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:background="@color/sport_background"
        android:fillViewport="true"
        android:padding="12dp">

        <LinearLayout
            android:id="@+id/chatContainer"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical" />

    </ScrollView>

    <!-- Input bar -->
    <LinearLayout
        android:id="@+id/messageBar"
        android:layout_width="match_parent"
        android:layout_height="74dp"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingStart="12dp"
        android:paddingEnd="12dp"
        android:paddingTop="10dp"
        android:paddingBottom="10dp"
        android:background="@color/sport_card">

        <EditText
            android:id="@+id/edtChatMessage"
            android:layout_width="0dp"
            android:layout_height="52dp"
            android:layout_weight="1"
            android:hint="Type your message..."
            android:singleLine="true"
            android:inputType="text"
            android:textSize="14sp"
            android:textColor="@color/sport_text_dark"
            android:textColorHint="@color/sport_text_muted"
            android:paddingStart="16dp"
            android:paddingEnd="16dp"
            android:background="@drawable/bg_input" />

        <Button
            android:id="@+id/btnSendMessage"
            android:layout_width="88dp"
            android:layout_height="52dp"
            android:text="Send"
            android:textAllCaps="false"
            android:textSize="13sp"
            android:textStyle="bold"
            android:textColor="#FFFFFF"
            android:layout_marginStart="8dp"
            android:background="@drawable/bg_button_primary"
            android:backgroundTint="@null" />

    </LinearLayout>

</LinearLayout>